var base = 12;
var altura = 40;

if ((base > 0) && (altura > 0)) {
    var area= ((base * altura)/ 2)
    console.log(`A area do triangulo é igual à ${area} metros².`)
} else {
    console.log(`Valores inseridos são inválidos, favor inserir um valor maior que zero.`)
}