var base = 90;
var altura = 60;

if ((base > 0) && (altura > 0)) {
    var area= (base*altura)
    console.log(`A area do retangulo é igual à ${area} metros².`)
} else {
    console.log(`Valores inseridos são inválidos, base e altura devem ter valores maiores que zero.`)
}