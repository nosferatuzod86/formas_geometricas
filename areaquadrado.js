var lado = 10;

if ((lado > 0)) {
    var area= (lado**2)
} else {
    console.log(`Valores inseridos devem ser maiores que zero.`)
    return
}
console.log(`A area do quadrado é igual à ${area} metros quadrados.`)